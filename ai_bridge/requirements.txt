# ── Web framework ──────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ── HTTP client (async, for MiniMax & NewsAPI) ─────────────────
httpx==0.27.2

# ── WebSocket client (cTrader Open API) ────────────────────────
websockets==13.1

# ── Settings / env ─────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ── Logging ────────────────────────────────────────────────────
loguru==0.7.2

# ── Macro data (DXY, US10Y) + backtest OHLCV ───────────────────
yfinance==0.2.50
numpy==2.1.3
pandas==2.2.3

# ── Scheduling (optional: periodic market context refresh) ─────
apscheduler==3.10.4

# ── Testing ────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0

# ── Backtest cross-check (OPTIONAL) ────────────────────────────
# Uncomment to enable vectorbt cross-check in simulator.metrics_via_vectorbt
# vectorbt==0.27.2
